# Backend runtime configuration for local development.
# Copy to backend/.env only when overriding defaults. Do not store secrets here.
APP_SERVICE_NAME=ai-analytics-platform
APP_SERVICE_VERSION=0.1.0
APP_ENVIRONMENT=development
APP_API_PREFIX=/api/v1
APP_DOCS_ENABLED=true
APP_LOG_LEVEL=INFO
