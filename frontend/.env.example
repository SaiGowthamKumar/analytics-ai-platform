# Base URL used by the browser API client during local development.
# Browser-local URLs may use localhost; containers communicate through service names.
VITE_API_BASE_URL=http://localhost:8000/api/v1
