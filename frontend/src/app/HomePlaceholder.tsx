export function HomePlaceholder(): React.JSX.Element {
  return (
    <section aria-labelledby="application-heading">
      <h1 id="application-heading">Application foundation</h1>
      <p>The presentation layer is ready for approved feature modules.</p>
    </section>
  );
}
